CREATE TABLE seat (
	table_id int not null primary key,
	xpos int not null,
	ypos int not null,
	mem_idx int REFERENCES member(mem_idx)
);

ALTER TABLE seat ALTER COLUMN table_id SERIAL PRIMARY KEY;

CREATE TABLE tableorder (
	table_id int REFERENCES seat(table_id),
	order_date time,
	product_code int REFERENCES food(product_code)
);

SELECT table_id, xpos, ypos FROM seat
WHERE ( SELECT mem_idx );

INSERT INTO tableorder VALUES (2, )







/* 사용법 */
System.DateTime.Now.ToString("yyyy");
DateTime.Now.ToString("yyyy-MM-dd-HH-mm-ss");
 
/* 예 */
DateTime.Now.ToString("yyyy");
// 2012년
 
DateTime.Now.ToString("MM");
// 10월
 
DateTime.Now.ToString("dd");
// 15일
 
DateTime.Now.ToString("hh");
// 15시
 
DateTime.Now.ToString("yyyyMMdd");
// 2012년 10월 15일
 
DateTime.Now.ToString("HHmmss");
//15시 02분 00초
 
DateTime.Now.ToString("yyyy-MM-dd");
//2012-10-15


출처: https://j07051.tistory.com/538 [흘러간다...]



<Button.Template>
        <ControlTemplate>
            <Border Name="border" CornerRadius="10" BorderThickness="3" BorderBrush="DarkGray" 
                    Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"
                    Width="100" Height="70">
                <Grid Margin="5,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="1*"/>

                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="txtTableName" Text="Table 1" FontSize="10"  HorizontalAlignment="Center"/>
                    <StackPanel Grid.Row="1" Name="txtTableMenuList">
                        <TextBlock Text="먹태구이" FontSize="8"/>
                        <TextBlock Text="노가리" FontSize="8"/>
                        <TextBlock Text="떡볶이" FontSize="8"/>
                    </StackPanel>
                    <TextBlock Grid.Row="2" Name="txtTablePrice" Text="20000원" FontSize="10"  HorizontalAlignment="Right"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Border.BorderBrush" Value="AliceBlue"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Button.Template>


CREATE TABLE menuorder (
    product_code    INT     NOT NULL    REFERENCES product(product_code),
    menu_count      INT     DEFAULT 1,
    discount_price  INT     DEFAULT 0,
    total_price     INT     NOT NULL,
    order_time      TIME
    table_id        INT     NOT NULL    REFERENCES seat(table_id)
);

